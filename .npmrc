# Keep dependency installation compatible with the repository's package graph.
# Do not inject environment-specific NODE_OPTIONS or preload modules into CI,
# local development, Vercel, or other build environments.
legacy-peer-deps=true
