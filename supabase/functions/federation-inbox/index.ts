import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2";
const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!;const SERVICE_ROLE=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")||Deno.env.get("SUPABASE_SECRET_KEY")||"";const admin=createClient(SUPABASE_URL,SERVICE_ROLE,{auth:{persistSession:false,autoRefreshToken:false}});const MAX_BODY=2_000_000,MAX_CLOCK_SKEW=300;const CTX=["https://www.w3.org/ns/activitystreams","https://w3id.org/security/v1"];const CORS={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"accept,content-type,date,digest,signature,host","Access-Control-Allow-Methods":"POST,OPTIONS"};const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...CORS,"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"}});const text=(v:unknown)=>typeof v==="string"?v:"";const uri=(v:unknown)=>typeof v==="string"?v:v&&typeof v==="object"?text((v as Record<string,unknown>).id):"";const now=()=>new Date().toISOString();const enc=(v:string)=>encodeURIComponent(v);const b64=(x:ArrayBuffer|Uint8Array)=>btoa(String.fromCharCode(...new Uint8Array(x)));
function pemToBytes(pem:string){const b=pem.replace(/-----BEGIN PUBLIC KEY-----|-----END PUBLIC KEY-----|\s/g,"");const bin=atob(b);return Uint8Array.from(bin,c=>c.charCodeAt(0));}
async function verifySignature(req:Request,raw:string,actorUri:string){const signature=req.headers.get("signature"),digest=req.headers.get("digest"),date=req.headers.get("date");if(!signature||!digest||!date)throw new Error("missing HTTP signature headers");const when=Date.parse(date);if(!Number.isFinite(when)||Math.abs(Date.now()-when)>MAX_CLOCK_SKEW*1000)throw new Error("stale Date header");const expected=`SHA-256=${b64(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(raw)))}`;if(digest!==expected)throw new Error("digest mismatch");const p:Record<string,string>={};for(const m of signature.matchAll(/([A-Za-z][A-Za-z0-9_-]*)="([^"]*)"/g))p[m[1].toLowerCase()]=m[2];if((p.algorithm||"").toLowerCase()!=="rsa-sha256"||!p.headers||!p.signature)throw new Error("unsupported HTTP signature");const keyId=p.keyid||actorUri;if(!keyId.startsWith("https://")||keyId.split("#")[0]!==actorUri)throw new Error("signature key owner does not match actor");const r=await fetch(actorUri,{headers:{accept:"application/activity+json, application/ld+json"}});if(!r.ok)throw new Error(`actor key fetch failed: ${r.status}`);const a=await r.json(),key=a.publicKey;if(!key?.publicKeyPem||(key.id&&key.id!==keyId))throw new Error("actor public key mismatch");const u=new URL(req.url);const signing=p.headers.split(/\s+/).map((name:string)=>{const l=name.toLowerCase();if(l==="(request-target)")return `(request-target): ${req.method.toLowerCase()} ${u.pathname}${u.search}`;const v=req.headers.get(name);if(v===null)throw new Error(`signed header missing: ${name}`);return `${l}: ${v}`;}).join("\n");const ck=await crypto.subtle.importKey("spki",pemToBytes(key.publicKeyPem),{name:"RSASSA-PKCS1-v1_5",hash:"SHA-256"},false,["verify"]);if(!await crypto.subtle.verify("RSASSA-PKCS1-v1_5",ck,Uint8Array.from(atob(p.signature),c=>c.charCodeAt(0)),new TextEncoder().encode(signing)))throw new Error("HTTP signature invalid");return{keyId,expiresAt:new Date(when+MAX_CLOCK_SKEW*1000).toISOString()};}
async function localActorForObject(objectUri:string){const {data,error}=await admin.from("federated_actors").or(`uri.eq.${enc(objectUri)},actor_url.eq.${enc(objectUri)}`).limit(1);if(error)throw error;return data?.[0]||null;}
async function remoteActor(actorUri:string){const r=await fetch(actorUri,{headers:{accept:"application/activity+json, application/ld+json"}});if(!r.ok)throw new Error(`remote actor fetch failed: ${r.status}`);return r.json();}
async function signAndSend(local:any,inbox:string,activity:any){if(!local?.private_key_jwk)throw new Error("local actor has no private signing key");const body=JSON.stringify(activity),u=new URL(inbox),date=new Date().toUTCString(),digest=`SHA-256=${b64(await crypto.subtle.digest("SHA-256",new TextEncoder().encode(body)))}`,key=await crypto.subtle.importKey("jwk",local.private_key_jwk,{name:"RSASSA-PKCS1-v1_5",hash:"SHA-256"},false,["sign"]),signing=`(request-target): post ${u.pathname}${u.search}\nhost: ${u.host}\ndate: ${date}\ndigest: ${digest}`,sig=b64(await crypto.subtle.sign("RSASSA-PKCS1-v1_5",key,new TextEncoder().encode(signing)));return fetch(inbox,{method:"POST",body,headers:{Date:date,Digest:digest,Host:u.host,Accept:"application/activity+json, application/ld+json","Content-Type":"application/activity+json",Signature:`keyId="${local.uri||local.actor_url}#main-key",algorithm="rsa-sha256",headers="(request-target) host date digest",signature="${sig}"`,"User-Agent":"Testagram-Federation/4.0"}});}
async function recordDelivery(local:any,activity:any,inbox:string,response:Response|null,error?:string){const {data}=await admin.from("federated_activities").insert({uri:activity.id,activity_type:activity.type,actor_uri:local.uri||local.actor_url,object_uri:uri(activity.object)||null,target_uri:uri(activity.target)||null,raw_activity:activity,received_at:now(),processed_at:now()}).select("id").single();if(!data?.id)return;await admin.from("federation_deliveries").upsert({activity_id:data.id,target_inbox:inbox,instance_domain:new URL(inbox).hostname,status:response?.ok?"delivered":"retry",attempt_count:1,next_attempt_at:new Date(Date.now()+60000).toISOString(),last_attempt_at:now(),last_status_code:response?.status||null,last_error:error||null,delivered_at:response?.ok?now():null,activity_payload:activity},{onConflict:"activity_id,target_inbox"});}
async function acceptOrReject(local:any,remoteActorUri:string,followActivity:any,accept:boolean){const remote=await remoteActor(remoteActorUri),inbox=remote.endpoints?.sharedInbox||remote.inbox;if(!inbox||!local)return;const activity={"@context":CTX,id:`${local.uri||local.actor_url}/activities/${crypto.randomUUID()}`,type:accept?"Accept":"Reject",actor:local.uri||local.actor_url,object:followActivity};try{const r=await signAndSend(local,inbox,activity);await recordDelivery(local,activity,inbox,r);}catch(e){await recordDelivery(local,activity,inbox,null,e instanceof Error?e.message:String(e));}}
async function processActivity(a:any,actorUri:string){const type=text(a.type),o=a.object,objectUri=uri(o),targetUri=uri(a.target);if(type==="Follow"){const local=await localActorForObject(objectUri||targetUri);if(!local)return;const blocked=await admin.from("federated_relationships").select("id").eq("local_user_id",local.user_id).eq("remote_actor_uri",actorUri).eq("relationship","blocked").eq("state","active").maybeSingle();const accept=!blocked.data;await admin.from("federated_relationships").upsert({local_user_id:local.user_id,remote_actor_uri:actorUri,relationship:"follower",state:accept?"active":"rejected",updated_at:now()},{onConflict:"local_user_id,remote_actor_uri,relationship"});await acceptOrReject(local,actorUri,a,accept);return;}if(type==="Accept"||type==="Reject"){const followed=uri(o);if(followed){const local=await localActorForObject(followed);if(local)await admin.from("federated_relationships").upsert({local_user_id:local.user_id,remote_actor_uri:actorUri,relationship:"following",state:type==="Accept"?"active":"rejected",updated_at:now()},{onConflict:"local_user_id,remote_actor_uri,relationship"});}return;}if(type==="Create"||type==="Update"){const obj=typeof o==="object"&&o?o:null;if(!obj||!uri(obj.id))return;const id=uri(obj.id);await admin.from("federated_objects").upsert({uri:id,object_type:text(obj.type)||"Object",actor_uri:uri(obj.attributedTo)||actorUri,url:text(obj.url)||id,content:text(obj.content)||text(obj.name)||null,summary:text(obj.summary)||null,published_at:obj.published?new Date(obj.published).toISOString():null,updated_at:obj.updated?new Date(obj.updated).toISOString():now(),sensitive:Boolean(obj.sensitive),in_reply_to_uri:uri(obj.inReplyTo)||null,quote_uri:uri(obj.quote)||null,attachments:Array.isArray(obj.attachment)?obj.attachment:[],tags:Array.isArray(obj.tag)?obj.tag:[],raw_object:obj,tombstone:false,deleted_at:null},{onConflict:"uri"});return;}if(type==="Like"||type==="Announce"){if(!objectUri)return;await admin.from("federated_interactions").upsert({activity_uri:text(a.id)||`urn:testagram:${crypto.randomUUID()}`,activity_type:type,actor_uri:actorUri,object_uri:objectUri,active:true,raw_activity:a,updated_at:now()},{onConflict:"activity_uri"});return;}if(type==="Undo"){const innerType=typeof o==="object"&&o?text((o as any).type):"";let referencedType=innerType;if(!referencedType&&objectUri){const q=await admin.from("federated_activities").select("activity_type").eq("uri",objectUri).maybeSingle();referencedType=q.data?.activity_type||"";}if(referencedType==="Follow"){await admin.from("federated_relationships").update({state:"removed",updated_at:now()}).eq("remote_actor_uri",actorUri).eq("relationship","follower");}else if(referencedType==="Like"||referencedType==="Announce"){await admin.from("federated_interactions").update({active:false,updated_at:now()}).eq("activity_uri",objectUri);}return;}if(type==="Delete"&&objectUri){await admin.from("federated_objects").update({tombstone:true,deleted_at:now(),raw_object:{id:objectUri,type:"Tombstone",formerType:"Note"},updated_at:now()}).eq("uri",objectUri);}}
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response(null,{status:204,headers:CORS});if(req.method!=="POST")return json({error:"POST required"},405);if(!/(activity\+json|ld\+json|application\/json)/i.test(req.headers.get("content-type")||""))return json({error:"Unsupported ActivityPub content type"},415);try{const raw=await req.text();if(!raw||raw.length>MAX_BODY)return json({error:"Invalid activity body"},400);const a=JSON.parse(raw) as Record<string,unknown>,type=text(a.type),actor=uri(a.actor);if(!type||!actor||!/^https:\/\//i.test(actor))return json({error:"Activity type and HTTPS actor are required"},400);const verified=await verifySignature(req,raw,actor),activityId=text(a.id)||`urn:testagram:inbound:${crypto.randomUUID()}`;const replay=await admin.from("federation_replays").insert({activity_uri:activityId,actor_uri:actor,signature_key_id:verified.keyId,expires_at:verified.expiresAt});if(replay.error){if(replay.error.code==="23505")return new Response(null,{status:202,headers:CORS});throw replay.error;}const persisted=await admin.from("federated_activities").upsert({uri:activityId,activity_type:type,actor_uri:actor,object_uri:uri(a.object)||null,target_uri:uri(a.target)||null,raw_activity:a,received_at:now(),processed_at:null,processing_error:null,processing_attempts:1},{onConflict:"uri"}).select("id").single();if(persisted.error)throw persisted.error;try{await processActivity(a,actor);await admin.from("federated_activities").update({processed_at:now(),processing_error:null}).eq("uri",activityId);}catch(e){const m=e instanceof Error?e.message:String(e);await admin.from("federated_activities").update({processing_error:m}).eq("uri",activityId);console.error("[federation-inbox] processing failure",m);}return new Response(null,{status:202,headers:CORS});}catch(e){console.error("[federation-inbox] rejected",e);return json({error:e instanceof Error?e.message:"Malformed ActivityPub request"},401);}});
