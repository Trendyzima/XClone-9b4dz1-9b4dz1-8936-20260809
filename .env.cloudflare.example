# Frontend: public Worker API URL only
VITE_CLOUDFLARE_API_URL=https://tsocial-api.<your-subdomain>.workers.dev

# Worker secrets are NOT placed in this file or Vite variables.
# Configure them with Wrangler:
# npx wrangler secret put RESEND_API_KEY --config cloudflare/wrangler.jsonc
# npx wrangler secret put AUTH_EMAIL_FROM --config cloudflare/wrangler.jsonc
