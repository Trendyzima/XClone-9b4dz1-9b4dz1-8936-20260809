name: Testagram Cloudflare API Security

on:
  push:
    branches: [main]
    paths:
      - 'cloudflare/**'
      - '.github/workflows/cloudflare-api-security.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  ZONE_NAME: testagram.site
  API_HOST: api.testagram.site
  FEDERATION_HOST: federation.testagram.site
  API_PATH_PREFIX: /api/

jobs:
  provision-api-exception:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Validate Cloudflare zone-security credentials
        env:
          CLOUDFLARE_ZONE_API_TOKEN: ${{ secrets.CLOUDFLARE_ZONE_API_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "$CLOUDFLARE_ZONE_API_TOKEN" ]; then
            echo 'Missing GitHub environment secret: CLOUDFLARE_ZONE_API_TOKEN'
            echo 'Required Cloudflare token permissions for this workflow: Zone Read, Zone WAF Edit, and Bot Management Edit.'
            echo 'The existing CLOUDFLARE_API_TOKEN is intentionally reserved for Worker deployment and returned HTTP 403 on zone Rulesets/Bot Management endpoints.'
            exit 1
          fi

      - name: Resolve Cloudflare zone
        id: zone
        env:
          CLOUDFLARE_ZONE_API_TOKEN: ${{ secrets.CLOUDFLARE_ZONE_API_TOKEN }}
        run: |
          set -euo pipefail
          zones="$(curl -fsS --max-time 20 "https://api.cloudflare.com/client/v4/zones?name=${ZONE_NAME}&status=active" -H "Authorization: Bearer ${CLOUDFLARE_ZONE_API_TOKEN}" -H 'Content-Type: application/json')"
          echo "$zones" | jq -e '.success == true and (.result | length) >= 1' >/dev/null
          zone_id="$(echo "$zones" | jq -r '.result[0].id')"
          test -n "$zone_id" && test "$zone_id" != "null"
          echo "zone_id=${zone_id}" >> "$GITHUB_OUTPUT"

      - name: Inspect Bot Fight Mode before applying federation exception
        env:
          CLOUDFLARE_ZONE_API_TOKEN: ${{ secrets.CLOUDFLARE_ZONE_API_TOKEN }}
          ZONE_ID: ${{ steps.zone.outputs.zone_id }}
        run: |
          set -euo pipefail
          status="$(curl -sS -o /tmp/bot-management.json -w '%{http_code}' --max-time 20 "https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/bot_management" -H "Authorization: Bearer ${CLOUDFLARE_ZONE_API_TOKEN}" -H 'Content-Type: application/json')"
          echo "Bot Management API HTTP status: ${status}"
          cat /tmp/bot-management.json
          test "$status" = 200
          jq -e '.success == true' /tmp/bot-management.json >/dev/null
          jq '{fight_mode, sbfm_likely_automated, sbfm_definitely_automated, sbfm_verified_bots, sbfm_static_resource_protection}' /tmp/bot-management.json || true

      - name: Provision or locate zone custom-rule entrypoint
        id: ruleset
        env:
          CLOUDFLARE_ZONE_API_TOKEN: ${{ secrets.CLOUDFLARE_ZONE_API_TOKEN }}
          ZONE_ID: ${{ steps.zone.outputs.zone_id }}
        run: |
          set -euo pipefail
          url="https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/rulesets/phases/http_request_firewall_custom/entrypoint"
          status="$(curl -sS -o /tmp/entrypoint.json -w '%{http_code}' --max-time 20 "$url" -H "Authorization: Bearer ${CLOUDFLARE_ZONE_API_TOKEN}" -H 'Content-Type: application/json')"
          cat /tmp/entrypoint.json || true
          if [ "$status" = "200" ]; then
            ruleset_id="$(jq -r '.result.id // empty' /tmp/entrypoint.json)"
            test -n "$ruleset_id"
          elif [ "$status" = "404" ]; then
            create_status="$(curl -sS -o /tmp/created-ruleset.json -w '%{http_code}' --max-time 20 -X POST "https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/rulesets" -H "Authorization: Bearer ${CLOUDFLARE_ZONE_API_TOKEN}" -H 'Content-Type: application/json' --data '{"name":"Testagram production federation security exceptions","description":"Scoped exceptions for Testagram API and federation hosts","kind":"zone","phase":"http_request_firewall_custom"}')"
            cat /tmp/created-ruleset.json || true
            test "$create_status" = "200"
            ruleset_id="$(jq -r '.result.id // empty' /tmp/created-ruleset.json)"
            test -n "$ruleset_id"
          else
            echo 'Cannot read/create Cloudflare custom-rule entrypoint.'
            exit 1
          fi
          echo "ruleset_id=${ruleset_id}" >> "$GITHUB_OUTPUT"

      - name: Install idempotent API and federation edge exceptions
        env:
          CLOUDFLARE_ZONE_API_TOKEN: ${{ secrets.CLOUDFLARE_ZONE_API_TOKEN }}
          ZONE_ID: ${{ steps.zone.outputs.zone_id }}
          RULESET_ID: ${{ steps.ruleset.outputs.ruleset_id }}
        run: |
          set -euo pipefail
          expression='((http.host eq "api.testagram.site" and starts_with(http.request.uri.path, "/api/")) or (http.host eq "federation.testagram.site"))'
          rules="$(curl -fsS --max-time 20 "https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/rulesets/${RULESET_ID}" -H "Authorization: Bearer ${CLOUDFLARE_ZONE_API_TOKEN}" -H 'Content-Type: application/json')"
          if echo "$rules" | jq -e --arg expr "$expression" '.result.rules[]? | select(.action == "skip" and .expression == $expr and ((.action_parameters.phases // []) | index("http_request_sbfm")) and ((.action_parameters.phases // []) | index("http_request_firewall_managed")) and ((.action_parameters.phases // []) | index("http_ratelimit")) and ((.action_parameters.products // []) | index("securityLevel")) and ((.action_parameters.products // []) | index("bic")))' >/dev/null; then
            echo 'Scoped API/federation edge exception already exists; no mutation required.'
            exit 0
          fi
          payload="$(jq -nc --arg expr "$expression" '{action:"skip",action_parameters:{phases:["http_request_sbfm","http_request_firewall_managed","http_ratelimit"],products:["securityLevel","bic"]},expression:$expr,description:"Allow Testagram API and ActivityPub federation traffic to reach the Worker without SBFM, managed-WAF, rate-limit, security-level, or browser-integrity challenge interference"}')"
          status="$(curl -sS -o /tmp/add-rule.json -w '%{http_code}' --max-time 20 -X POST "https://api.cloudflare.com/client/v4/zones/${ZONE_ID}/rulesets/${RULESET_ID}/rules" -H "Authorization: Bearer ${CLOUDFLARE_ZONE_API_TOKEN}" -H 'Content-Type: application/json' --data "$payload")"
          cat /tmp/add-rule.json || true
          test "$status" = "200"
          jq -e '.success == true' /tmp/add-rule.json >/dev/null

      - name: Verify API and federation hosts are not challenge-blocked
        run: |
          set -euo pipefail
          for attempt in 1 2 3 4 5 6; do
            api_status="$(curl -sS -o /tmp/api-health.json -w '%{http_code}' --max-time 20 'https://api.testagram.site/api/health' || true)"
            federation_status="$(curl -sS -o /tmp/federation-health.json -w '%{http_code}' --max-time 20 'https://federation.testagram.site/api/health' || true)"
            echo "Attempt ${attempt}: API=${api_status} FEDERATION=${federation_status}"
            if [ "$api_status" = "200" ] && [ "$federation_status" = "200" ]; then
              jq -e '.ok == true and .databaseReachable == true and .jwksReachable == true and .r2Binding == true' /tmp/api-health.json >/dev/null
              jq -e '.ok == true and .federation == true and .databaseReachable == true and .jwksReachable == true and .r2Binding == true' /tmp/federation-health.json >/dev/null
              exit 0
            fi
            sleep 5
          done
          cat /tmp/api-health.json || true
          cat /tmp/federation-health.json || true
          echo 'API or federation host is still challenge-blocked or unhealthy.'
          exit 1
